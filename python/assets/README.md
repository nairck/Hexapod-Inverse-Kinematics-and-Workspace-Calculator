# assets/

Optional branding for the executable.

- Put an icon file named **`app.ico`** here and it will be used automatically as the
  Windows `.exe` icon (the PyInstaller spec picks it up if present).
- Nothing else is required; the app runs fine without anything in this folder.
